"use strict";
const crypto=require("node:crypto");
const RULESET_VERSION="initial-governance-bootstrap-authorization-evidence-v0.2.0";
const GOVERNANCE_ACT="INITIAL_GOVERNANCE_BOOTSTRAP";
const OUTCOMES=Object.freeze({ACCEPTED:"INITIAL_GOVERNANCE_BOOTSTRAP_AUTHORIZATION_EVIDENCE_ACCEPTED",ALREADY_ACCEPTED:"INITIAL_GOVERNANCE_BOOTSTRAP_AUTHORIZATION_EVIDENCE_ALREADY_ACCEPTED",REJECTED:"INITIAL_GOVERNANCE_BOOTSTRAP_AUTHORIZATION_EVIDENCE_REJECTED",STALE:"INITIAL_GOVERNANCE_BOOTSTRAP_AUTHORIZATION_EVIDENCE_STALE",UNCERTAIN:"INITIAL_GOVERNANCE_BOOTSTRAP_AUTHORIZATION_EVIDENCE_UNCERTAIN",CONFLICT:"INITIAL_GOVERNANCE_BOOTSTRAP_AUTHORIZATION_EVIDENCE_CONFLICT"});
const plain=v=>Boolean(v&&typeof v==="object"&&!Array.isArray(v));const nonEmpty=v=>typeof v==="string"&&v.length>0;const clone=v=>v==null?v:JSON.parse(JSON.stringify(v));
function canonicalize(v){if(Array.isArray(v))return v.map(canonicalize);if(plain(v))return Object.keys(v).sort().reduce((o,k)=>(o[k]=canonicalize(v[k]),o),{});return v;}const canonicalStringify=v=>JSON.stringify(canonicalize(v));const digest=v=>`sha256:${crypto.createHash("sha256").update(canonicalStringify(v)).digest("hex")}`;
function result(outcome,reason,evidence=null){return Object.freeze({outcome,reason:reason||null,evidence:clone(evidence),authority:"NONE",humanAuthorizationCreated:outcome===OUTCOMES.ACCEPTED||outcome===OUTCOMES.ALREADY_ACCEPTED,mutationAuthorized:false,mutationPerformed:false});}
function validGate(g){return plain(g)&&g.type==="INITIAL_GOVERNANCE_BOOTSTRAP_GATE"&&nonEmpty(g.gateId)&&nonEmpty(g.gateRevision)&&g.governanceAct===GOVERNANCE_ACT&&nonEmpty(g.repositoryIdentity)&&nonEmpty(g.targetPath)&&nonEmpty(g.beforeStateId)&&nonEmpty(g.afterStateId)&&g.oneTime===true&&g.status==="PENDING"&&nonEmpty(g.gateEvidenceRef)&&g.authority==="NONE";}
function validComposed(a){return plain(a)&&a.type==="BOOTSTRAP_APPROVAL_COMPOSED_EVIDENCE"&&nonEmpty(a.composedApprovalEvidenceId)&&a.governanceAct===GOVERNANCE_ACT&&a.decision==="APPROVE"&&nonEmpty(a.principalRef)&&nonEmpty(a.bindingId)&&nonEmpty(a.sourceEventRef)&&nonEmpty(a.bootstrapGateId)&&nonEmpty(a.bootstrapGateRevision)&&nonEmpty(a.repositoryIdentity)&&nonEmpty(a.targetPath)&&nonEmpty(a.beforeStateId)&&nonEmpty(a.afterStateId)&&a.oneTime===true&&a.authority==="NONE";}
function createInitialGovernanceBootstrapAuthorizationEvidence({bootstrapGatePort,composedApprovalEvidencePort,bootstrapAuthorizationLedger}){
 for(const [n,p] of Object.entries({bootstrapGatePort,composedApprovalEvidencePort}))if(typeof p!=="function")throw new TypeError(`${n} must be a function`);for(const n of ["findByGateId","findByBindingId","commit"])if(!bootstrapAuthorizationLedger||typeof bootstrapAuthorizationLedger[n]!=="function")throw new TypeError(`bootstrapAuthorizationLedger.${n} must be a function`);
 function accept(request){const f=["rulesetVersion","composedApprovalEvidenceId","bootstrapGateId","bootstrapGateRevision","repositoryIdentity","targetPath","expectedBeforeStateId","expectedAfterStateId"];if(!plain(request)||Object.keys(request).length!==f.length||!f.every(k=>nonEmpty(request[k]))||request.rulesetVersion!==RULESET_VERSION)return result(OUTCOMES.REJECTED,"unsupported request schema or ruleset");
  let gate,approval;try{gate=bootstrapGatePort({gateId:request.bootstrapGateId});approval=composedApprovalEvidencePort({composedApprovalEvidenceId:request.composedApprovalEvidenceId});}catch(_){return result(OUTCOMES.UNCERTAIN,"bootstrap prerequisite unavailable");}
  if(!validGate(gate))return result(OUTCOMES.UNCERTAIN,"bootstrap gate not established");if(!validComposed(approval))return result(OUTCOMES.UNCERTAIN,"composed bootstrap approval evidence not established");
  const expected={gateId:request.bootstrapGateId,gateRevision:request.bootstrapGateRevision,repositoryIdentity:request.repositoryIdentity,targetPath:request.targetPath,beforeStateId:request.expectedBeforeStateId,afterStateId:request.expectedAfterStateId};
  for(const [k,v] of Object.entries(expected)){if(gate[k]!==v)return result(OUTCOMES.STALE,`gate ${k} mismatch`);const ak=k==="gateId"?"bootstrapGateId":k==="gateRevision"?"bootstrapGateRevision":k;if(approval[ak]!==v)return result(OUTCOMES.STALE,`composed approval ${ak} mismatch`);}
  let byGate,byBinding;try{byGate=bootstrapAuthorizationLedger.findByGateId(gate.gateId);byBinding=bootstrapAuthorizationLedger.findByBindingId(approval.bindingId);}catch(_){return result(OUTCOMES.UNCERTAIN,"bootstrap authorization ledger unavailable");}if(!Array.isArray(byGate)||!Array.isArray(byBinding))return result(OUTCOMES.UNCERTAIN,"bootstrap authorization ledger invalid");if(byGate.length>1||byBinding.length>1)return result(OUTCOMES.CONFLICT,"multiple bootstrap authorization records");
  const material={type:"INITIAL_GOVERNANCE_BOOTSTRAP_AUTHORIZATION_EVIDENCE",schemaVersion:"1.0",rulesetVersion:RULESET_VERSION,governanceAct:GOVERNANCE_ACT,principalRef:approval.principalRef,bindingId:approval.bindingId,sourceEventRef:approval.sourceEventRef,composedApprovalEvidenceId:approval.composedApprovalEvidenceId,bootstrapGateId:gate.gateId,bootstrapGateRevision:gate.gateRevision,repositoryIdentity:gate.repositoryIdentity,targetPath:gate.targetPath,beforeStateId:gate.beforeStateId,afterStateId:gate.afterStateId,gateEvidenceRef:gate.gateEvidenceRef,oneTime:true,consumed:false,authority:"NONE"};const evidence={...material,bootstrapAuthorizationId:digest(material)};const prior=byGate[0]||byBinding[0]||null;if(prior)return canonicalStringify(prior)===canonicalStringify(evidence)?result(OUTCOMES.ALREADY_ACCEPTED,"same exact bootstrap authorization already accepted",prior):result(OUTCOMES.CONFLICT,"bootstrap gate or binding already used by different authorization");let stored;try{stored=bootstrapAuthorizationLedger.commit(evidence);}catch(_){return result(OUTCOMES.CONFLICT,"bootstrap authorization ledger commit conflict");}if(canonicalStringify(stored)!==canonicalStringify(evidence))return result(OUTCOMES.CONFLICT,"bootstrap authorization ledger did not preserve exact evidence");return result(OUTCOMES.ACCEPTED,null,evidence);
 }return Object.freeze({accept});
}
module.exports=Object.freeze({RULESET_VERSION,GOVERNANCE_ACT,OUTCOMES,canonicalStringify,createInitialGovernanceBootstrapAuthorizationEvidence});
